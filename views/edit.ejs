<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Shopping List</title>
    <!-- <link rel="stylesheet" type="text/css" href="/css/style.css" /> -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <!-- <style>
      #sub-div {
        display: none;
      }
    </style> -->
  </head>
  <body>
    <center>
      <h1>Edit Shopping List</h1>
      <form class="w-50" action="/edititem/<%= edit._id %>" method="post">
        <div>
          <label class="form-label" for="tas">Enter Item:</label>
          <input
            type="text"
            value="<%=edit.item %>"
            name="item"
            required
            class="form-control"
          />

          <label class="form-label">Price:</label>
          <input
            type="number"
            name="price"
            required
            value="<%=edit.price%>"
            class="form-control"
            id="price"
            oninput="subCal()"
          />

          <label class="form-label">Quantity:</label>
          <input
            type="number"
            name="quantity"
            required
            class="form-control"
            value="<%=edit.quantity %>"
            oninput="subCal()"
            id="quantity"
          />
          <div id="sub-div">
            <span>Sub Total: N</span>
            <span id="sub-total"><%= edit.subTotal %></span>
          </div>
        </div>
        <button class="btn bg-dark-subtle text-white mt-2" type="submit">
          Edit Item
        </button>
      </form>
    </center>
  </body>
</html>
<script>
  let subD = document.getElementById("sub-div");
  let subT = document.getElementById("sub-total");
  subCal = () => {
    let quantity = document.getElementById("quantity").value;
    let price = document.getElementById("price").value;
    console.log(price);
    if (price == "" || quantity == "") {
    }

    subT.innerText = price * quantity;
  };
</script>
